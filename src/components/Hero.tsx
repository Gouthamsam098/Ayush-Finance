import HeroContainer from './HeroContainer';

export default function Hero() {
  return (
    <div className="relative flex-1 h-full overflow-y-auto lg:overflow-hidden">
      <HeroContainer />
    </div>
  );
}
